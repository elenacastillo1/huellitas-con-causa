<% layout('layouts/layout') %> <% title = 'Mis Mascotas | Huellitas con Causa'
%> <% description = 'Consulta tus mascotas registradas y descarga sus códigos QR
para mantenerlas seguras.' %> <%- include("partials/header", { usuario, rol })
%>

<div class="container">
  <h2>🐾 Mis mascotas registradas</h2>

  <% if (registros.length === 0) { %>
  <p>
    No tienes mascotas confirmadas aún. Una vez que tu pago sea validado por el
    administrador, aparecerán aquí 🐶💖
  </p>
  <% } else { %>
  <ul class="lista-datos">
    <% registros.forEach(m => { %>
    <li>
      <strong>Nombre:</strong> <%= m.nombreMascota %><br />
      <strong>Dueño:</strong> <%= m.nombreDueno %><br />
      <strong>Teléfono:</strong> <%= m.telefonoDueno %><br />
      <strong>Dirección:</strong> <%= m.direccionDueno %><br />
      <a href="/qr/<%= m._id %>.png" download class="btn-main">
        Descargar QR
      </a>
    </li>
    <% }) %>
  </ul>
  <% } %>
</div>

<%- include("partials/footer") %>
