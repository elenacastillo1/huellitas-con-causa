<% layout('layouts/layout') %> <% title = 'Login | Huellitas con Causa' %> <%
description = 'Accede a tu cuenta en Huellitas con Causa para comprar productos
solidarios y gestionar tus mascotas.' %> <%- include("partials/header") %>

<div class="container">
  <h2>🔐 Iniciar Sesión</h2>

  <% if (typeof error !== "undefined" && error) { %>
  <p class="error"><%= error %></p>
  <% } %> <% if (typeof success !== "undefined" && success) { %>
  <p class="success"><%= success %></p>
  <% } %>

  <form action="/auth/login" method="POST" class="formulario">
    <label for="email">Correo electrónico:</label>
    <input type="email" name="email" value="<%= email || '' %>" required />

    <label for="password">Contraseña:</label>
    <input type="password" name="password" required />

    <button type="submit" class="btn-main">Ingresar</button>
  </form>

  <p style="margin-top: 15px">
    ¿No tienes cuenta? <a href="/auth/register">Regístrate aquí</a>
  </p>
</div>

<%- include("partials/footer") %>
